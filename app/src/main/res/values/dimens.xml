<resources>
    <!-- Default screen margins, per the Android Design guidelines. -->
    <dimen name="activity_horizontal_margin">16dp</dimen>
    <dimen name="activity_vertical_margin">16dp</dimen>

    <!-- Default button width, height. -->
    <dimen name="btn_default_width">160dp</dimen>
    <dimen name="btn_default_height">50dp</dimen>

    <!-- Default edit text padding. -->
    <dimen name="et_default_padding">10dp</dimen>

    <!-- Default margin top for textview, button, edittext. -->
    <dimen name="tv_default_margin_top">15dp</dimen>
    <dimen name="et_default_margin_top">5dp</dimen>
    <dimen name="btn_default_margin_top">20dp</dimen>

    <!-- Default font size. -->
    <dimen name="font_size_small">12sp</dimen>
    <dimen name="font_size_normal">15sp</dimen>
    <dimen name="font_size_large">20sp</dimen>

    <!-- Default radio group padding. -->
    <dimen name="rg_padding_left">10dp</dimen>
    <dimen name="rg_custom_padding_left">8dp</dimen>
    <dimen name="text_view_radio_padding_left">8dp</dimen>
    <dimen name="ll_padding_left">18dp</dimen>

    <!-- Default color picker size -->
    <dimen name="color_picker_width">0dp</dimen>
    <dimen name="color_picker_height">60dp</dimen>


</resources>
